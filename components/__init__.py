from os.path import dirname, join

path = dirname(dirname(__file__))
assets_path = join(path, 'assets')
font_path = join(path, "assets", "fonts")
image_path = join(path, "assets", "images")
icon_path = join(font_path, "materialdesignicons-webfont.ttf")
